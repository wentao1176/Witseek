# Witseek v0.4.5

## 修复

- 修复选择尚不存在的安装目录时，NSIS 将安装目录、更新缓存、dsh 数据和工作区解析为空路径并误报重叠的问题。
- 安装与卸载都会在比较路径前规范化目录；路径无法解析时显示原始路径并停止，避免误删数据。
- 保留自定义安装路径、Witseek Coding preset、文件预览和 Git 改动视图。

安装程序仍为免管理员权限的未签名安装包。Windows SmartScreen 可能仍会提示保护；这与本次路径修复无关。
